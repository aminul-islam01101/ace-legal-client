import React from 'react';
import Slider from './slider/Slider';

const Home = () => {
    console.log('home');

    return (
        <div>
            <Slider />
        </div>
    );
};

export default Home;
